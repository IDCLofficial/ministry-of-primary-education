export const metadata = {
    title: 'Reset Password - MOPSE',
    description: 'Reset your password for MOPSE portal',
};

export default function ResetPasswordLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {children}
        </>
    );
}