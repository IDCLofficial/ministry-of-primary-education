export const metadata = {
    title: 'Forgot Password - MOPSE',
    description: 'Forgot your password for MOPSE portal',
};

export default function ForgotPasswordLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {children}
        </>
    );
}